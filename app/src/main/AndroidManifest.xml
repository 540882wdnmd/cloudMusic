<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:name=".utils.base.BaseApplication"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CloudMusic"
        android:hardwareAccelerated="true"
        tools:targetApi="tiramisu"
        android:testOnly="false">

        <service
            android:name=".utils.service.MusicServiceOnBind"
            android:enabled="true"
            android:exported="true"/>

        <activity android:name=".centre.playList.PlayListActivity"
            android:exported="true"/>
        <activity
            android:name=".centre.CentreActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:hardwareAccelerated="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>